#pragma once

#include <string.h>

class StrToHex
{
private:
    /* data */
public:
    void Mac(char* Mac,unsigned char * ret);
    void IP(char* Ip,unsigned char * ret);
    StrToHex(/* args */);
    ~StrToHex();
};


